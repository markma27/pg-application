# Production (Option B): set in Vercel for the web project.
# API on subdomain — no trailing slash.
# NEXT_PUBLIC_API_URL=https://api.pg-application-theta.vercel.app
# NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your-key
